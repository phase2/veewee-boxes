# Install veewee with bundler to work around the following error when trying to
# install it as a gem:
#
# ERROR:  While executing gem ... (Gem::DependencyError)
#    Unable to resolve dependencies: fog requires net-scp (~> 1.1)
#
# Using bundler allows it to install its own dependencies
##
source "https://rubygems.org"
gem 'veewee'
